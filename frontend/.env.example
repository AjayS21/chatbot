# Frontend (SvelteKit server) environment variables
#
# Copy to `frontend/.env` for local dev:
#   Copy-Item frontend\.env.example frontend\.env
#
# The SvelteKit server-side proxy routes under `/api/chat/*` use this URL
# to reach the backend service.

BACKEND_URL="http://localhost:3001"
